using UnityEngine;
using System.Collections;

public class ThemeChange : MonoBehaviour {
    private GameObject load;
	// Use this for initialization
	void Start () {
      //  myRenderer = GetComponent<Renderer>();
      //  gameObject.GetComponent<TextMesh>().text = "new text";
    //    load.AddComponent<Renderer>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    public void changeTheme()
    {
       // gameObject.renderer.material.color = Color.yellow;
        //GameObject.Find("LoadButton").GetComponent(MeshRenderer);
      //  load = GameObject.Find("Canvas/LoadButton");
       // if(load != null) load.renderer.material.color = Color.yellow;
        
    }

}
